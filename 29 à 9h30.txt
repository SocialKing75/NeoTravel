========================================================================
NEOTRAVEL — POINT D'AVANCEMENT
Note du 29/06/2026 (reprise prévue le 30/06 à 9h30)
========================================================================

------------------------------------------------------------------------
1) CE QUI A ÉTÉ FAIT AUJOURD'HUI
------------------------------------------------------------------------

A. Template de devis -> composant React
   - Récupéré le template devis.html (était sur la branche
     origin/feature/R5-data-airtable, commit fdf1c29).
   - Converti en composant React intégré à l'app Next.js :
       * src/components/DevisCard.tsx        (composant typé <DevisCard devis={...} />)
       * src/components/DevisCard.module.css (styles fidèles : bleu #0b1d33, vert #97cc04)
   - Montants formatés fr-FR, initiales client auto, TVA/validité paramétrables.
   - TVA = 10 % (conforme aux consignes).

B. API de prix (matrice) derrière le tunnel Cloudflare
   - URL : https://publicity-stripes-kitty-lay.trycloudflare.com
   - Endpoint réel : POST /calculer-devis   (PAS /calculer)
   - L'API attend l'ANCIEN schéma :
       { distance_km, aller_retour, saison, days_to_departure, capacity }
   - Réponse enveloppée : { status:"success", data:{ prix_ht, prix_ttc, ... } }
   - API confirmée OK : HTTP 200, TVA 10 % (ex. TTC 1518 € pour le payload test).
   - Script de test ajouté : test-pricing-tunnel.sh  (à la racine NeoTravel)
       Lancer :  bash test-pricing-tunnel.sh

C. Nœuds n8n corrigés (V1.json) — type @n8n/n8n-nodes-langchain.toolCode
   calculerDevis :
     - Calcule la distance LUI-MÊME dans n8n :
         géocodage Nominatim -> distance routière OSRM (HTTPS) -> saison selon mois
         -> days_to_departure -> POST /calculer-devis -> déballe data.
     - Conversion booléenne robuste de allerRetour via toBool() :
         "Aller-Retour"/"A/R"/"oui" -> true ; "Aller simple"/"non" -> false
     - Garde-fou > 85 passagers.
   Corrections de fond appliquées aux DEUX nœuds (extraireInfos + calculerDevis) :
     - SUPPRESSION de process.env (interdit dans le sandbox toolCode -> "process is not defined").
     - OSRM passé en HTTPS (le http:// peut être bloqué).
     - RETOUR en STRING obligatoire : JSON.stringify(...) au lieu de [{ json: {...} }]
       (sinon erreur "Wrong output type returned: should be a string but it is an object").
   Versions de référence des nœuds (au cas où) :
     scratchpad calculerDevis.js / extraireInfos.js

------------------------------------------------------------------------
2) LE BLOCAGE PRINCIPAL — À RÉGLER DEMAIN (30/06)
------------------------------------------------------------------------

PROBLÈME :
   gemma4:latest (Ollama) NE SUPPORTE PAS le tool-calling.
   => L'agent n'appelle JAMAIS calculerDevis.
   => Preuve : les logs du PC du tunnel ne montrent QUE des "GET /" (aucun POST /calculer-devis).
   => L'agent HALLUCINE un prix différent à chaque fois (980 €, 1800 €...),
      en violation de la règle "INTERDIT ABSOLU d'inventer un prix".
   => Aucune correction de code/prompt ne réglera ça tant que le modèle ne gère pas les outils.

FIX = changer le modèle du nœud agent pour un modèle compatible function-calling.
   Options (par ordre de reco) :
     1. Qwen2.5 7B   -> ollama pull qwen2.5:7b-instruct   (local, gratuit, RECOMMANDÉ)
     2. Claude Haiku 4.5 -> via le nœud Vercel AI Gateway (déjà présent, désactivé) ; cloud, payant
     3. Llama 3.1 8B -> ollama pull llama3.1:8b            (local, gratuit, un cran en dessous)

   Dès le modèle choisi : modifier V1.json (2 min) + durcir le prompt anti-hallucination
   ("si calculerDevis absent/erreur -> ne JAMAIS donner de prix, signaler un souci technique").

------------------------------------------------------------------------
3) STACK / RAPPELS
------------------------------------------------------------------------
   - Frontend : Next.js 16, TypeScript, Tailwind        (localhost:3000)
   - Agent IA : n8n (V1.json) + Ollama                  (localhost:5678)
   - Pricing  : API derrière tunnel Cloudflare (voir §B) ; api.py local = version "propre" NON déployée
   - CRM      : Airtable base appTP3CebBC8FfZYq

   IMPORTANT n8n : RÉIMPORTER V1.json après chaque modif.
   IMPORTANT git : c'est l'utilisateur qui push lui-même (lui donner les commandes).

------------------------------------------------------------------------
4) À COMMITTER (quand tu veux — toi qui push)
------------------------------------------------------------------------
   git add src/components/DevisCard.tsx src/components/DevisCard.module.css pricing_engine.py test-pricing-tunnel.sh
   git commit -m "Add DevisCard component + pricing tunnel test script"
   (V1.json est hors du repo, dans GIT/ -> non concerné par ce commit.)

========================================================================
REPRENDRE ICI DEMAIN : choisir le modèle (§2) -> modifier V1.json -> retester.
========================================================================
